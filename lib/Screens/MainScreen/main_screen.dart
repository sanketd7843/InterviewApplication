import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/MainScreen/components/body.dart';

class MainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
